name: choppa
channels:

  - conda-forge
  - defaults

dependencies:
    # Base depends
  - python
  - pip
  - pandas
  - biopython
  - click
  - pydantic

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

