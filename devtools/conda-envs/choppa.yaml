name: choppa
channels:

  - conda-forge
  - bio-conda
  - defaults

dependencies:
  # Base depends
  - python=3.11 # pinned for augur deps
  - pip

  # Data workflowing
  - pandas
  - biopython
  - click
  - mdanalysis

  # CLI
  - click
  
  # visualizations
  - pymol-open-source
  - pyqt
  - prolif
  - logomaker
  - jinja2

  # Testing
  - pytest
  - pytest-cov
  - codecov

  # docs
  - sphinx
  - sphinx_rtd_theme

  # nextstrain
  - altair
  - augur