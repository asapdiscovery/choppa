name: choppa
channels:

  - conda-forge
  - defaults

dependencies:
  # Base depends
  - python
  - pip

  # Data workflowing
  - pandas
  - biopython
  - click
  - mdanalysis

  # CLI
  - click
  
  # visualizations
  - pymol-open-source
  - pyqt
  - prolif
  - logomaker
  - jinja2

  # Testing
  - pytest
  - pytest-cov
  - codecov

  # docs
  - sphinx
  - sphinx_rtd_theme 
  
  # Pip-only installs
  #- pip:
  #  - codecov

